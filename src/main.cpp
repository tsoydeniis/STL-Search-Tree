#include <iostream>

#include "search_tree.h"

#include <set>


int main() {
    //SearchTree<int, in_order_tag> tree;
    // std::set<int> tree;
    // tree.insert(10);
    // tree.insert(5);
    // tree.insert(15);

    // auto [lower, upper] = tree.equal_range(10);

    // auto [lower2, upper2] = tree.equal_range(15);

    // std::cout << *lower2 << std::endl; 
    // std::cout << *upper2 << std::endl;
}